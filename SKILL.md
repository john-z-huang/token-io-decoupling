---
name: luna-output-delegation
description: "开发任务（Codex 或其他支持 Agent Skills 的 code agent；阅读项目、诊断、规划、实现、长输出、修复、验证）：遵循会话级模型分工与 gpt-5.6-luna 委派流程。"
---

# Luna Output Delegation

本技能用于执行一项模型分工实验。它提供调度约定，不能绕过更高优先级的权限、用户授权、产品限制或安全规则，也不能硬性拦截运行时行为。

## 分工规则

- 当前模型不是明确可确认的 `gpt-5.6-luna` 时，只做阅读、规划、极简调度和验证；不要亲自实施代码、文档、配置或其他实质性产出。
- 所有实现工作，以及可能产生大量文本、代码、数据或其他输出的工作，都必须显式委派给模型为 `gpt-5.6-luna` 的子 Agent。修复、返工和根据验证结果的后续修改同样委派给 Luna。
- 委派请求应尽量短，只包含目标、必要约束和相关文件路径；优先让子 Agent 直接读取文件，不在 prompt 中复制大段内容。根据任务复杂度选择上下文策略：简单任务使用精简提示和最小必要上下文；复杂任务按下文共享完整上下文。执行输出任务的委派应显式指定 `model=gpt-5.6-luna` 和 `reasoning_effort=xhigh`；子 Agent 不得再次递归委派。
- Luna 子 Agent 自己执行任务时不得再次递归委派；它应直接完成实现并把结果写入指定文件或工作区。
- 只有互不依赖、不会争用相同写入目标的任务才可并行。存在依赖、共享文件或需要前一步结果时，按顺序调度。

## 增量进度反馈

- 创建 Luna 时，父 Agent 必须在提示词中明确要求 Luna：每完成若干有意义的任务事项或阶段，就主动使用 `send_message` 向创建它的父 Agent 返回简短进度，至少覆盖已完成事项、当前工作、验证或问题、下一步。按实际阶段发送即可，不必机械地固定频率汇报。
- Luna 不得长时间无反馈，也不要发送没有实质内容的心跳；遇到阻塞、失败或重大偏差时，应立即汇报。
- 父 Agent 优先依赖这些增量消息掌握执行状况，只在必要时读取更完整的上下文或产物，避免为了确认进度而全量读取子 Agent 上下文。

## 简单诊断任务：精简上下文

- 高级别父 Agent 需要通过 `pwd`、`git status`、`git log`、`git diff`、`ls`、`find`、`rg --files` 或同类只读命令查看当前目录、仓库或项目现状时，必须把这项简单诊断委派给一个新的 `gpt-5.6-luna` Agent；父 Agent 不应亲自运行这些命令。
- 这类任务不需要共享完整会话上下文。提示词只写清要检查的目标、必要路径、只读约束和需要返回的事实，例如“检查 `/path/to/project` 的 Git 工作区状态，报告未提交变更和当前分支”。不要复制长对话、规划或文件内容；若接口支持 `fork_turns`，可使用 `none` 或最小上下文。
- Luna Agent 只执行诊断并返回简洁、可核验的结果，不修改文件、不递归委派。父 Agent 根据返回结果判断项目现状是否符合预期；需要进一步查看时，再发出范围明确的诊断请求。
- 简单、局部且自包含的其他任务也应采用同样的“新建 Luna Agent + 精简提示 + 最小上下文”策略；一旦任务依赖较多上下文、多个文件或需要连续决策，就升级为复杂任务流程。

## 复杂任务的规划循环

- 若输出任务涉及多个依赖、较多文件、较长流程或明显的不确定性，先通过完整共享上下文启动 Luna 的规划阶段，不要立即要求实现。提示词只需说明“分析当前上下文并输出规划方案”，让 Luna 直接读取已有上下文。
- 高级别父 Agent 负责审查规划方案：检查目标覆盖、约束、依赖、步骤顺序、验收标准和风险。若不符合要求，使用简短、针对性的反馈提示同一个 Luna Agent 修改规划；重复“审查→简短反馈→修改”循环，直到方案合格。规划阶段的每次委派同样必须明确使用 `gpt-5.6-luna`、`reasoning_effort=xhigh` 和完整共享上下文。
- 规划方案通过审查后，再向同一个 Luna Agent 发出执行指令，并明确要求按已批准方案实施。执行完成后，父 Agent 仍按“输出与验收”流程检查结果；发现问题时继续委派给同一个 Luna Agent 修复并重新验收。
- 任务简单且无需先拆解时，可直接启动新的 Luna Agent 执行；使用精简提示和最小必要上下文，不启动规划循环。只有被判断为复杂的输出任务才需要完整共享上下文和规划循环。

## 身份与不可用处理

在调度前确认子 Agent 的模型身份和必要参数。若 `gpt-5.6-luna` 不可用、模型身份未知或无法满足显式模型要求，停止实质性工作并给出简短阻塞说明；不要自行改用其他模型或把实现留给当前模型。复杂任务若无法开启完整上下文共享，也应停止并报告阻塞；简单诊断任务不受此限制，可使用最小上下文。若接口无法设置 `reasoning_effort=xhigh`，仅在需要执行复杂输出任务时阻塞；简单诊断仍可按可用参数委派。价格或成本只是用户提出的假设动机，不得表述为已实测或已验证的价格结论。

## 输出与验收

当最终答复较长时，让 Luna 将完整答复保存为用户指定的文件；父 Agent 只提供文件链接和极简摘要，不复述或重写长文。由父 Agent 负责阅读必要的结果、综合并报告验证结论；验收所需的 shell 只读检查也按“简单诊断任务”另行委派给 Luna。验证失败时仍将修复委派给同一个明确的 `gpt-5.6-luna` 子 Agent。

## 加载边界

本技能默认允许自动发现；普通 Skill 的 `description` 只影响隐式匹配，不能保证每次启动完整加载。若要保证 Codex 每次运行遵循核心规则，应将其放入全局 `~/.codex/AGENTS.md` 或由宿主注入 system/developer instructions；其他支持 Agent Skills 的 code agent 需按各自持久指令机制配置。
