# 仓库指令

多语言文档要求统一维护在 [`MULTI_LINGUAL_zh_cn.md`](MULTI_LINGUAL_zh_cn.md) 中。

任何在本仓库中创建、修改、重命名、移动或删除文档的 Agent 和贡献者，都必须在进行文档变更前加载并遵守该文件。

## 文档归属与原子化

- 每个受维护的 Markdown 文档必须只有一个清晰、可复用的责任。文件名、标题、开头的归属声明、章节和链接必须描述同一个责任。
- 如果文档组合了可独立复用的概念，必须拆成独立的英文与 `_zh_cn.md` owner 文档对。不能为了少建文件而保留复杂的反向概念组合。
- 拆分后不得残留重复的旧章节或旧尾部。原文档只能保留其声明的 owner 责任，被移动的内容只能归属于新 owner。
- Layer-01 文档负责基础概念；Layer-02 文档负责单个工作流检查点或窄范围检查点 owner；Layer-03 文档可以把低层 owner 组合成路线，但不得重新定义低层策略。
- 路线组合文档可以列出并排序 owner，但不能成为模式门禁、委派生命周期、运行环境能力、修复执行、验证独立性/报告/epoch 或其他低层概念的第二个 owner。
- 新 owner 文件及其组合入口必须在同一变更集中更新。英文和简体中文文件必须保持语义镜像，约束强度和动作要求等价。

## Markdown 引用与层级方向

- 文档引用必须使用真实的相对 Markdown 链接。不得用 HTML、裸路径或反引号伪链接替换有效 Markdown 链接来规避校验。
- 应鼓励合法的 Markdown 引用。验证器只应拒绝不存在、跨语言或违反层级方向的引用，不得重新引入“禁止所有合法 Markdown 链接”的宽泛规则。
- 每个新增或实质处理的双语 owner 文档都应在顶部附近包含可见的语言切换链接，并在存在相关 owner 时包含 `Related concepts`/`相关概念`，使用同语言 Markdown 链接。
- 英文文档链接英文目标；简体中文文档链接 `_zh_cn.md` 目标。跨语言链接只用于可见的语言切换。
- 遵循当前 `scripts/check-doc-layer-links.py` 的方向规则：普通 Layer-01 引用指向 Layer-01；普通 Layer-02 引用指向 `references/` 或 Layer-01；普通 Layer-03 引用指向共享协议、Layer-01 或 Layer-02。不得用链接格式绕过这些规则。
- 每个本地 Markdown 目标都必须存在，且链接必须指向当前 owner 文件名，不能继续使用拆分前的旧文件名。

## Worktree 与子代理文档工作流

- 文档原子化或重构任务在要求使用 worktree 时，必须使用隔离 worktree。子代理不得修改主工作区。
- 按小范围文档切片工作。子代理先只读取指定 worktree 文件并返回分析；主代理审查边界后发送最终修改指令；子代理再只修改获准切片。
- 子代理必须遵循最小读取原则：不得频繁全量扫描文档，也不得读取主工作区中没有改动的 Skill 文档。只读取当前 worktree 中完成该切片所需的文件及其直接链接目标。
- 每个获准切片完成后，主代理必须在本地提交前检查文件名、责任边界、双语镜像和链接。提交保持小范围并使用中文提交信息。除非明确要求，不得推送到 GitHub。
- 批次准备最终验收时，使用独立的只读校验 agent。它不得修复、暂存、提交或推送，只报告文件名/内容不一致、关系违规、旧 owner 名称和断链。
- 保留无关用户修改，不得暂存仓库生成或既有未跟踪状态（例如 `.serena/`），除非明确要求。

## Skill 迭代清晰度审阅指令

当根指令明确要求进行 Skill 文档的迭代清晰度审阅时，在新的隔离 Git worktree 中使用以下可复用顺序：

1. 创建一个低推理强度的子 Agent，只向它分配指定的 Skill worktree。子 Agent 首轮只读取当前 worktree 的 Skill 入口、工作流以及审阅所需的直接 reference，不修改文件。
2. 要求子 Agent 反馈有证据支持的歧义、矛盾、缺失的执行边界或令人困惑的段落，并给出准确文件路径、标题、后果和最小修复方向。子 Agent 不得递归创建代理，也不得联系无关 Session。
3. 主 Agent 审核反馈，形成 Semantic Contract 和一个窄范围的获批准文档切片，然后明确派发路径、允许的修改、返回条件和未发布边界。子 Agent 只能修改该切片，并返回修改路径、diff 摘要、检查结果和剩余不确定性。
4. 主 Agent 按 Contract、ownership 边界、英文/中文镜像、链接和范围审核子 Agent 的结果。如果未通过，不得提交；只发送窄范围修复指令并继续复审。如果通过，要求子 Agent 运行适用的文档检查，只暂存获批准文件，并使用中文提交信息创建一个本地 commit。除非根指令另行授权，不得推送到远端。
5. 每次获批准的提交后，要求子 Agent 重新阅读当前 worktree 中的 Skill 文档，并反馈是否仍有实质性歧义或可执行改进。持续执行“主 Agent 审核 → 子 Agent 修复 → 审核通过 → 提交”循环，直到子 Agent 报告没有剩余的实质性困惑或可执行改进，且主 Agent 独立接受该结论。
6. 完成时核验最终 worktree、提交范围、双语镜像、必需检查和不存在远程影响。保留无关改动；除非明确请求清理，否则保留该 worktree。

本指令只控制文档审阅循环，不授权无关实现、历史重写、远程操作或超出获批准文档切片的修改。

## 必需验证

每个文档切片完成前，必须从仓库根目录运行：

```bash
python3 scripts/check-multilingual-docs.py
python3 scripts/check-doc-layer-links.py
git diff --check
```

还必须人工检查已修改的本地 Markdown 目标、语言镜像、owner 文件名，以及不存在 HTML 或伪链接。如果验证器与当前文档规则冲突，必须同时更新验证器和受影响文档，不能绕过检查。
